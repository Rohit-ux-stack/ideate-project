<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="bg-[#0F172A] text-slate-200 min-h-screen flex font-sans">
    <%- include('../partials/sidebar') %>
    <main class="ml-64 flex-1 flex items-center justify-center p-8">
        <div class="w-full max-w-2xl space-y-8">
            
            <div class="bg-slate-900/50 border border-slate-800 rounded-3xl p-8 shadow-2xl">
                <h2 class="text-3xl font-bold mb-6 text-white border-b border-slate-800 pb-4">Profile Settings</h2>
                
                <% if (typeof message !== 'undefined' && message) { %>
                    <div class="<%= message.type === 'success' ? 'bg-green-500/10 text-green-400 border-green-500/20' : 'bg-red-500/10 text-red-400 border-red-500/20' %> p-4 rounded-xl mb-6 text-sm font-bold text-center border">
                        <%= message.text %>
                    </div>
                <% } %>

                <form action="/settings/update" method="POST" enctype="multipart/form-data" class="space-y-6">
                    <div class="flex items-center space-x-6">
                        <div class="relative group">
                            <% if (user.image) { %>
                                <img src="<%= user.image %>" class="w-24 h-24 rounded-full object-cover border-4 border-slate-800 group-hover:border-blue-500 transition">
                            <% } else { %>
                                <div class="w-24 h-24 rounded-full bg-slate-800 flex items-center justify-center text-3xl font-bold text-slate-500 border-4 border-slate-800">
                                    <%= user.displayName.charAt(0) %>
                                </div>
                            <% } %>
                            
                            <label class="absolute inset-0 bg-black/50 rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 cursor-pointer transition">
                                <i class="ri-camera-line text-white text-2xl"></i>
                                <input type="file" name="profileImage" class="hidden" onchange="this.form.submit()">
                            </label>
                        </div>
                        <div>
                            <h3 class="font-bold text-lg"><%= user.displayName %></h3>
                            <p class="text-slate-500 text-sm">Click the image to update photo</p>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-bold text-slate-500 mb-2 uppercase">Display Name</label>
                            <input type="text" name="displayName" value="<%= user.displayName %>" class="w-full bg-[#161B22] border border-slate-700 p-3 rounded-xl focus:border-blue-500 outline-none text-white">
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-slate-500 mb-2 uppercase">Theme</label>
                            <select name="theme" class="w-full bg-[#161B22] border border-slate-700 p-3 rounded-xl focus:border-blue-500 outline-none text-white">
                                <option value="dark" <%= user.preferences && user.preferences.theme === 'dark' ? 'selected' : '' %>>Dark Mode</option>
                                <option value="light" <%= user.preferences && user.preferences.theme === 'light' ? 'selected' : '' %>>Light Mode</option>
                            </select>
                        </div>
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-slate-500 mb-2 uppercase">Bio</label>
                        <textarea name="bio" rows="3" class="w-full bg-[#161B22] border border-slate-700 p-3 rounded-xl focus:border-blue-500 outline-none text-white resize-none" placeholder="Tell us about yourself..."><%= user.bio %></textarea>
                    </div>

                    <div class="pt-4 border-t border-slate-800">
                        <h3 class="text-sm font-bold text-slate-400 uppercase tracking-wider mb-4">Contacts</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                             <div class="flex items-center bg-[#161B22] border border-slate-700 rounded-xl px-3"><i class="ri-mail-line text-slate-400 text-lg"></i><input type="email" name="email" value="<%= user.contacts ? user.contacts.email : '' %>" class="w-full bg-transparent border-none text-sm text-slate-200 p-3 focus:ring-0" placeholder="Public Email"></div>
                            <div class="flex items-center bg-[#161B22] border border-slate-700 rounded-xl px-3"><i class="ri-linkedin-fill text-slate-400 text-lg"></i><input type="text" name="linkedin" value="<%= user.contacts ? user.contacts.linkedin : '' %>" class="w-full bg-transparent border-none text-sm text-slate-200 p-3 focus:ring-0" placeholder="LinkedIn URL"></div>
                            <div class="flex items-center bg-[#161B22] border border-slate-700 rounded-xl px-3"><i class="ri-github-fill text-slate-400 text-lg"></i><input type="text" name="github" value="<%= user.contacts ? user.contacts.github : '' %>" class="w-full bg-transparent border-none text-sm text-slate-200 p-3 focus:ring-0" placeholder="GitHub URL"></div>
                            <div class="flex items-center bg-[#161B22] border border-slate-700 rounded-xl px-3"><i class="ri-global-line text-slate-400 text-lg"></i><input type="text" name="website" value="<%= user.contacts ? user.contacts.website : '' %>" class="w-full bg-transparent border-none text-sm text-slate-200 p-3 focus:ring-0" placeholder="Website"></div>
                            <div class="flex items-center bg-[#161B22] border border-slate-700 rounded-xl px-3"><i class="ri-twitter-x-line text-slate-400 text-lg"></i><input type="text" name="twitter" value="<%= user.contacts ? user.contacts.twitter : '' %>" class="w-full bg-transparent border-none text-sm text-slate-200 p-3 focus:ring-0" placeholder="X (Twitter) URL"></div>
                        </div>
                    </div>

                    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white py-3 rounded-xl font-bold shadow-lg shadow-blue-900/40 transition">Save Changes</button>
                </form>
            </div>

            <div class="bg-slate-900/50 border border-slate-800 rounded-3xl p-8 shadow-xl">
                <h3 class="text-xl font-bold text-white mb-6 flex items-center gap-2"><i class="ri-lock-password-line text-blue-500"></i> Change Password</h3>
                <form action="/settings/change-password" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <input type="password" name="currentPassword" placeholder="Current Password" required class="bg-[#161B22] border border-slate-700 p-3 rounded-xl text-white focus:border-blue-500 outline-none">
                    <input type="password" name="newPassword" placeholder="New Password" required class="bg-[#161B22] border border-slate-700 p-3 rounded-xl text-white focus:border-blue-500 outline-none">
                    <button type="submit" class="md:col-span-2 bg-slate-800 hover:bg-slate-700 text-white py-3 rounded-xl font-bold transition">Update Password</button>
                </form>
            </div>

            <div class="bg-red-500/5 border border-red-500/20 rounded-3xl p-8 shadow-xl">
                <h3 class="text-xl font-bold text-red-500 mb-2 flex items-center gap-2"><i class="ri-alert-line"></i> Danger Zone</h3>
                <p class="text-slate-400 text-sm mb-6">Deleting your account is irreversible. All your ideas, comments, and profile data will be permanently removed.</p>
                <form action="/settings/delete-account" method="POST" onsubmit="return confirm('Are you strictly sure? This cannot be undone.');">
                    <button type="submit" class="w-full bg-red-600/10 hover:bg-red-600 text-red-500 hover:text-white border border-red-500/20 hover:border-transparent py-3 rounded-xl font-bold transition">Delete My Account</button>
                </form>
            </div>

        </div>
    </main>
</body>
</html>