<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="bg-[#0F172A] text-slate-200 min-h-screen flex font-sans">
    <%- include('../partials/sidebar') %>

    <main class="ml-64 flex-1 p-8">
        <div class="text-center mb-12">
            <h1 class="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-yellow-600 mb-2">Hall of Fame 🏆</h1>
            <p class="text-slate-400">Top Innovators creating the most impact.</p>
        </div>

        <div class="max-w-4xl mx-auto space-y-4">
            <% users.forEach((u, index) => { %>
                <div class="bg-slate-900/50 border border-slate-800 p-4 rounded-2xl flex items-center gap-4 relative group hover:border-yellow-500/30 transition">
                    
                    <div class="text-3xl font-black text-slate-700 w-12 text-center shrink-0 select-none">
                        #<%= index + 1 %>
                    </div>

                    <% if (index === 0) { %>
                        <div class="absolute top-0 right-0 p-2 bg-yellow-500/10 rounded-bl-xl border-b border-l border-yellow-500/20">
                            <i class="ri-vip-crown-fill text-xl text-yellow-400"></i>
                        </div>
                    <% } %>
                    <% if (index === 1) { %>
                        <div class="absolute top-0 right-0 p-2 bg-slate-700/20 rounded-bl-xl border-b border-l border-slate-600/50">
                            <i class="ri-medal-fill text-xl text-slate-300"></i>
                        </div>
                    <% } %>
                    <% if (index === 2) { %>
                        <div class="absolute top-0 right-0 p-2 bg-orange-700/10 rounded-bl-xl border-b border-l border-orange-700/20">
                            <i class="ri-medal-fill text-xl text-orange-600"></i>
                        </div>
                    <% } %>

                    <div class="w-14 h-14 rounded-full border-2 border-slate-700 p-0.5 bg-slate-900 overflow-hidden shrink-0">
                        <% if (u._id.image) { %>
                            <img src="<%= u._id.image %>" class="w-full h-full rounded-full object-cover">
                        <% } else { %>
                            <div class="w-full h-full rounded-full bg-slate-800 flex items-center justify-center text-lg font-bold"><%= u.authorName.charAt(0) %></div>
                        <% } %>
                    </div>

                    <div class="flex-1 min-w-0">
                        <h3 class="text-lg font-bold text-white flex items-center gap-2 truncate">
                            <%= u._id.displayName %>
                            <% if (index < 3) { %> 
                                <span class="text-[10px] bg-yellow-500/10 text-yellow-400 px-1.5 py-0.5 rounded border border-yellow-500/20 uppercase font-bold tracking-wider shrink-0">Top Rated</span> 
                            <% } %>
                        </h3>
                        <p class="text-slate-500 text-sm truncate"><%= u._id.bio || "Available for collaboration." %></p>
                    </div>

                    <div class="flex gap-6 text-right mr-4 md:mr-10 shrink-0">
                        <div>
                            <div class="text-xl font-bold text-green-400"><%= u.totalUpvotes %></div>
                            <div class="text-[10px] text-slate-500 uppercase tracking-wider font-bold">Total Upvotes</div>
                        </div>
                        <div>
                            <div class="text-xl font-bold text-blue-500"><%= u.totalIdeas %></div>
                            <div class="text-[10px] text-slate-500 uppercase tracking-wider font-bold">Ideas</div>
                        </div>
                    </div>
                    
                    <a href="/user/<%= u._id._id %>" class="absolute inset-0 z-20"></a>
                </div>
            <% }) %>
        </div>
    </main>
</body>
</html>