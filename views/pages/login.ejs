<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Ideate</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style> body { font-family: 'Inter', sans-serif; } </style>
</head>
<body class="bg-[#0F172A] text-slate-200 min-h-screen flex items-center justify-center p-4">

    <div class="w-full max-w-md bg-[#11161D] border border-slate-800 p-8 rounded-3xl shadow-2xl">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-blue-500 italic mb-2">IDEATE</h1>
            <p class="text-slate-500">Welcome back, Innovator.</p>
        </div>

        <% if (locals.error) { %>
            <div class="bg-red-500/10 border border-red-500/20 text-red-400 px-4 py-3 rounded-xl mb-6 text-sm font-bold text-center">
                ⚠️ <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST" class="space-y-6">
            <div>
                <label class="block text-xs font-bold text-slate-500 mb-2 uppercase">Email Address</label>
                <input type="email" name="email" class="w-full bg-[#161B22] border border-slate-800 p-4 rounded-xl focus:border-blue-500 outline-none text-white transition" required>
            </div>

            <div>
                <label class="block text-xs font-bold text-slate-500 mb-2 uppercase">Password</label>
                <input type="password" name="password" class="w-full bg-[#161B22] border border-slate-800 p-4 rounded-xl focus:border-blue-500 outline-none text-white transition" required>
            </div>

            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white py-4 rounded-xl font-bold transition shadow-lg shadow-blue-900/40">
                Sign In
            </button>
        </form>

        <p class="text-center mt-8 text-slate-500 text-sm">
            New here? <a href="/signup" class="text-blue-400 font-bold hover:underline">Create an account</a>
        </p>
    </div>
</body>
</html>