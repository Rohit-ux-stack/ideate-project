<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>
<body class="bg-[#0F172A] flex items-center justify-center min-h-screen p-4 font-sans">
    
    <div class="w-full max-w-md bg-slate-900 border border-slate-800 p-8 rounded-3xl shadow-2xl relative text-center">
        
        <div class="w-20 h-20 bg-blue-500/10 text-blue-500 rounded-full flex items-center justify-center text-4xl mx-auto mb-6 border border-blue-500/20">
            <i class="ri-mail-check-line"></i>
        </div>

        <h1 class="text-2xl font-bold text-white mb-2">Check your Email</h1>
        <p class="text-slate-400 text-sm mb-6">We've sent a 4-digit code to <br> <span class="text-white font-bold"><%= email %></span></p>

        <% if (error) { %>
            <div class="bg-red-500/10 border border-red-500/20 text-red-400 p-3 rounded-xl mb-6 text-sm font-bold">
                <%= error %>
            </div>
        <% } %>

        <form action="/verify-otp" method="POST" class="space-y-6">
            
            <div>
                <input type="text" name="otp" maxlength="4" placeholder="0000" class="w-full bg-slate-950 border border-slate-800 rounded-2xl px-4 py-4 text-center text-white text-3xl tracking-[1rem] focus:border-blue-500 outline-none transition font-mono placeholder-slate-700">
            </div>

            <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 rounded-xl transition shadow-lg shadow-blue-900/20">
                Verify Account
            </button>
        </form>

        <div class="mt-6">
            <a href="/signup" class="text-slate-500 text-sm hover:text-white transition">Entered wrong email?</a>
        </div>
    </div>

</body>
</html>